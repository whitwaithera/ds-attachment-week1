# Week 1 Learning Log
Date: sept 9 - Sept 1
Attachee: Whitney Waithera Wanjiru

# 1. Concepts Learned
*   Python Basics: Learned how to use variables, data types (int, float, string, boolean), and collections (lists, dictionaries, sets, tuples).
*   Control Flow: Practiced using `if/elif/else` statements and `for/while` loops to iterate through data.
*   Functions: Learned how to write reusable code using `def`, passing arguments, and returning values.
*   Error Handling:Understood how to use `try/except` blocks to prevent the program from crashing due to user input errors (like typing a letter instead of a number).
*   Environment Setup: Successfully set up Python, VS Code, Git, and GitHub. Learned how to create and activate a virtual environment (`venv`).

# 2. Challenges Faced
    Challenge: I got an error when trying to run my Python script in the terminal.
    *   Solution:I realized I forgot to activate my virtual environment first. I learned to run `source venv/bin/activate` (Mac) / `venv\Scripts\activate` (Windows) before running my code.*   Challenge: Formatting the output into a CSV file was confusing at first.
    * Solution:I read the Python documentation on the `csv` module and learned how to use `csv.DictWriter` to properly write headers and rows.

# 3. Practical Application
*   Completed the Week 1 exercises covering Python fundamentals.
*    Built a "Student Data Processor" script that takes raw data, calculates averages, filters passing students, and saves the results to a new CSV file (`passing_students.csv`).

*   Built a "Student Data Processor" script that takes raw data, calculates averages, filters passing students, and saves the results to a new CSV file (`passing_students.csv`).
